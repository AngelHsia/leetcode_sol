#include <iostream>
using namespace std;

class Node {
public:
    int val;
    Node* next;
    Node* random;
    
    Node(int _val) {
        val = _val;
        next = NULL;
        random = NULL;
    }
};

class Solution {
public:
    Node* copyRandomList(Node* head) {
        Node* og_current_node = head;
        Node* new_nodes(0);
        while(og_current_node != NULL){
            
            return 0;
        }
    }
};

int main(){
    return 0;
}
